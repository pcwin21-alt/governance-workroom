# 활동 기록 속성 모델

## 목적

회의, 정책제안, 이슈, 운영 메모, 인수인계처럼 서로 다른 활동을 하나의 기록 체계로 찾고, 분류하고, 아카이브·전달용 파일로 전환한다. 노션의 범용 데이터베이스를 복제하지 않는다. 청년참여기구 운영에서 판단의 맥락과 다음 행동을 보존하는 데 필요한 속성만 지원한다.

## 1. 기록의 단위

`원본 기록`은 회의·정책제안·이슈·공지·리포트의 실제 작업 화면이다. 원본을 만들거나 바꾸면 `기록 파일`이 자동 생성·갱신된다. 기록 파일에는 원본 본문, 속성 스냅샷, 버전, 작성·수정 이력이 함께 남는다.

`아카이브 파일`은 기록 파일 또는 원본 기록을 특정 시점에 보관한 사본이다. 원본이 후에 바뀌어도 아카이브 파일의 당시 속성과 본문은 변하지 않는다. DOCX·PDF·통합 자료는 이 파일 버전을 기준으로 만든다.

```
회의 / 정책제안 / 이슈 / 운영 메모
              ↓ 자동 생성·갱신
       기록 파일 (본문 + 속성 + 버전)
              ↓ 선택 보관 또는 전달용 고정
       아카이브 파일 / DOCX / PDF / 활동자료집
```

## 2. 기본 속성

아래 속성은 모든 기록 파일에 기본으로 표시한다. `자동` 속성은 삭제할 수 없고, 화면 목록에서만 숨길 수 있다. 나머지는 위원장이 이름·선택지를 조정하거나, 필요 없으면 사용 중지할 수 있다.

| 속성 | 형식 | 값의 출처 | 아카이빙·파일화 용도 |
| --- | --- | --- | --- |
| 기록명 | 텍스트 | 원본 제목 | 파일명·문서 제목 |
| 기록 유형 | 단일 분류 | 회의·정책제안·이슈·공지·메모·인계 | 보관 폴더와 문서 양식 선택 |
| 작성자 | 사람, 자동 | 원본 작성자 | 책임·인계 맥락 |
| 작성일 / 최근 수정 | 날짜, 자동 | 시스템 | 버전·변경 이력 |
| 주 분류 | 단일 분류 | 7색 기록 구분 | 목록 필터·문서 묶음 |
| 진행 상태 | 단일 분류 | 초안·진행·검토·완료·보관 | 다음 행동과 미완료 기록 찾기 |
| 정책 의제 | 다중 분류 | 주거·일자리·참여 등 | 활동 리포트·주제별 검색 |
| 담당자 | 사람 | 담당 위원 또는 간사 | 마감·후속 확인 |
| 참여자·관련 기구 | 다중 사람 / 관계 | 회의 참석자·공동작성자·분과 | 공동 작업과 인수인계 |
| 기준일·마감일 | 날짜 | 확인일·회의일·제출 기한 | 일정·기한 정렬 |
| 다음 행동 | 텍스트 | 운영자가 입력 | 다음 회의·인계 목록 |
| 근거·원문 | URL | 공식 자료 또는 원문 | 공무원 전달 시 출처 추적 |
| 사진·첨부 | 파일·미디어 참조 | 회의 사진·제출 파일 | 보관 파일의 첨부 목록 |
| 수치 | 숫자 | 참석 인원·결정 수·예산 등 | 리포트 집계. 개인 평가에는 사용하지 않음 |
| 공개 범위 | 단일 분류 | 위원회 내부·승인 대기·공개 가능 | 사례 공개와 개인정보 경계 |
| 관련 기록 | 관계 | 회의 ↔ 제안 ↔ 이슈 ↔ 인계 | 문서 패키지·맥락 탐색 |
| 기록 ID / 버전 / 보관 시각 | 자동 | 시스템 | 파일 추적·재현 |

## 3. 추가·삭제 규칙

위원장과 간사는 `기록 > 기록 정리 > 속성 설정`에서 다음 속성만 추가한다.

- 텍스트
- 단일 분류
- 다중 분류
- 날짜
- 숫자
- 체크
- URL

선택지에는 이름과 색상을 둘 수 있다. 사용자 정의 속성을 삭제하면 과거 파일의 값은 즉시 파기하지 않고 `비표시 속성`으로 보존한다. 이미 생성된 아카이브·DOCX·PDF의 재현성을 지키기 위해서다.

사람, 관계, 사진·첨부, 생성·수정 시각, ID, 버전은 기록 구조와 권한·파일 보존에 연결되므로 1차 구현에서는 시스템 속성으로 유지한다. 수식·롤업·자동화 버튼·전화번호·이메일은 초기 범위에서 제외한다.

## 4. 화면 구성

대시보드의 목록은 모든 속성을 표로 늘어놓지 않는다. 기본 열은 `주 분류 / 기록명 / 작성자 / 진행 상태 / 최근 수정`으로 고정하고, 사용자가 필요한 두 개의 보조 열만 추가해서 본다.

기록을 열면 오른쪽 또는 상세 패널에 전체 속성을 표시한다. 수정 권한자는 속성 값을 바로 바꾸고, `속성 설정`에서는 필드명·형식·선택지·표시 여부를 관리한다. 이 설정 화면은 위원장·간사에게만 보인다.

## 5. 권한과 개인정보 경계

- 일반 위원: 본인이 작성하거나 참여 권한이 있는 기록의 허용된 속성 값만 수정한다.
- 간사·위원장: 기구 속성 정의, 분류 체계, 기록 속성을 관리한다.
- 사진·첨부와 사람 속성은 같은 참여기구 구성원에게만 표시한다.
- 주민등록번호, 전체 생년월일, 개인 연락처, 개인 활동 점수·순위는 속성으로 만들지 않는다.

## 6. 파일화 규칙

기록 파일의 각 버전은 `본문 + 당시 속성 목록 + 연결 기록 + 작성·수정 이력`을 함께 저장한다. 따라서 DOCX·PDF·활동자료집을 만들 때 선택한 버전의 제목·분류·작성자·기준일·출처·담당자·공개 범위를 문서 첫머리에 자동 표기할 수 있다.

아카이브 파일은 원본을 다시 읽어 최신 상태로 덮어쓰지 않는다. `보관본 갱신`을 누른 경우에만 새 아카이브 버전을 만든다. 이 규칙으로 당시 공무원에게 전달한 문서와 이후 수정본을 구분한다.

## 7. 구현 순서

1. 테넌트별 속성 스키마와 위 기본 속성의 시드 데이터를 만든다.
2. 기록 파일·아카이브 파일에 속성 값을 저장하고, 원본 변경·수동 속성 변경 모두 새 버전을 남긴다.
3. 기록 모아보기의 열 선택·필터와 기록 상세의 속성 패널을 연결한다.
4. 속성 설정에서 사용자 정의 필드와 선택지를 추가·숨김·삭제한다.
5. DOCX·PDF·통합 자료 생성 시 속성 스냅샷을 문서 메타데이터로 출력한다.

## 수락 기준

- 회의 또는 정책제안 한 건의 속성을 바꾸면 기록 파일의 새 버전이 생긴다.
- 같은 기록을 아카이빙한 뒤 원본을 바꿔도 기존 보관본의 속성은 바뀌지 않는다.
- 파일 내보내기 결과에 기록명·유형·분류·작성자·작성일·최근 수정·선택한 추가 속성이 표시된다.
- 다른 테넌트의 속성 정의·값·첨부·아카이브 파일은 읽거나 수정할 수 없다.
