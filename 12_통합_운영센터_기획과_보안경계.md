# 통합 운영센터 기획과 보안 경계

## 결정

`/admin`은 거버넌스 워크룸의 일반 업무 공간과 분리된 플랫폼 운영자 전용 통합 운영센터다. 첫 화면은 두 서비스의 연결 상태, 운영 신호, 권한 원칙, 감사 기록을 함께 확인하는 `통합 운영센터`다.

## 서비스 경계

| 서비스 | 현재 연결 | 관리자에게 보이는 범위 | 금지선 |
| --- | --- | --- | --- |
| 거버넌스 워크룸 | 연결됨 | 테넌트, 구성원·권한, 회의·제안 파이프라인, 동의·보존·감사 | 개인 열성도 점수화, 자동 평가, 테넌트 간 자료 노출 |
| 청년투게더 | 연결 대기 | 연결 상태와 연결 요건만 표시 | 가공·추정한 브리핑 수, 품질, Notion 동기화 성공 표시 |

청년투게더 연결은 읽기 전용 전용 서비스 계정, 최소 범위 토큰, 상태 API 계약, 실패·지연·부분 완료 상태가 정해진 뒤에만 켠다. 수집, 품질 판정, 웹/DB/내보내기, Notion 동기화는 독립적으로 표시한다.

## 권한 모델

1. 플랫폼 운영자는 각 서비스의 업무상 필요한 운영 지표와 보안 상태만 본다.
2. 서비스별 권한은 자동 병합하지 않는다. 워크룸의 테넌트 역할은 청년투게더의 편집·발행 권한이 아니다.
3. 서버는 화면 노출 여부와 별개로 매 요청에서 역할, 테넌트, 자원 관계를 확인한다.
4. 권한 변경, 테넌트 설정, 연구·데이터 요청, 향후 연결 설정은 감사 로그에 수행자·자원·시각을 남긴다. 감사 로그에 비밀값이나 개인정보 본문을 남기지 않는다.

## 적용된 웹 보안 기본값

- `HttpOnly`, `SameSite=Lax` 세션 쿠키를 사용하고 브라우저 저장소에 세션 토큰을 보관하지 않는다.
- 운영 배포에서는 `SESSION_COOKIE_SECURE=true`로 Secure 쿠키를 강제한다.
- 쿠키 기반 변경 요청은 동일 출처 Origin을 확인한다. API 테스트·서비스 간 인증은 Bearer 토큰을 쓸 수 있으나 실제 외부 연동은 별도 서비스 계정과 최소 권한을 사용해야 한다.
- CSP, frame 차단, MIME 추측 방지, referrer·permissions 정책을 기본 응답 헤더로 적용한다.

## 실제 운영 전 미완료 항목

이 MVP는 실제 개인정보나 기관 운영에 사용할 수 없다. 실제 도입 전에는 관리형 DB·키 관리·HTTPS 종단·MFA/SSO·침입 탐지·속도 제한·독립 보안 테스트·백업/파기·개인정보 처리 역할 및 보존 정책을 확정해야 한다.
