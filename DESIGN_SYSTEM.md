# 청년거버넌스 워크룸 디자인 가치관

이 문서는 워크룸 내부 제품 화면의 디자인 정본이다. 개별 화면의 취향보다 아래 원칙을 우선하며, 공개 홈페이지의 브랜드 표현은 별도 규칙으로 다룬다.

## 한 문장

> 청년의 제안이 누구에게 넘어갔고, 언제까지 무엇을 해야 하며, 어떤 근거와 결정이 남았는지를 조용하고 분명하게 보여 주는 운영 도구를 만든다.

## 여섯 가지 가치

### 1. 기록이 장식보다 앞선다

- 그래프·일러스트·큰 카드보다 회의, 제안, 담당자, 기한, 결정 사유를 먼저 보여 준다.
- 시각 요소는 이해 속도를 높일 때만 쓴다. 빈 공간을 채우기 위한 이미지는 넣지 않는다.
- 활동량을 흥미롭게 보이게 만드는 대신, 실제 처리 이력이 빠짐없이 남게 한다.

### 2. 다음 행동은 하나부터 보인다

- 각 화면의 주 행동은 원칙적으로 하나다.
- 보조 행동은 해당 객체의 행 또는 상세 화면 안에 둔다.
- 버튼은 `보기`, `관리`, `처리`가 아니라 `참석 응답`, `검토 의견 남기기`, `통합 자료 만들기`처럼 대상과 결과를 함께 쓴다.

### 3. 책임·시간·근거를 붙여 보여 준다

- 상태만 표시하지 않고 담당자, 기한, 마지막 변경을 함께 제공한다.
- 정책제안은 게시물이 아니라 `근거 회의 → 담당 부서 → 검토 → 결정 → 회신`의 이력 객체로 표현한다.
- 기록을 다음 기수와 행정 담당자가 다시 읽어도 맥락을 복원할 수 있어야 한다.

### 4. 공공 업무의 차분함을 지킨다

- 과장된 환영 문구, 게임화, 열성도 순위, 근거 없는 KPI를 사용하지 않는다.
- 색은 의미를 보조할 뿐 의미 자체가 되지 않는다. 상태는 기호와 텍스트를 함께 쓴다.
- 밝은 바탕, 얇은 선, 절제된 청록을 기본으로 하며 위험·지연·완료 색은 제한적으로 쓴다.

### 5. 설명은 줄이고 복구 가능성은 늘린다

- 메뉴명 아래 설명은 한 줄만 둔다. 같은 뜻의 제목과 소개문을 반복하지 않는다.
- 권한, 빈 상태, 오류, 삭제 영향, 복구 행동처럼 없으면 사고가 생기는 문장은 남긴다.
- 사용자가 실수할 수 있는 변경은 확인·감사 로그·되돌릴 수 있는 경로 중 하나를 제공한다.

### 6. 신뢰 경계를 화면에 드러낸다

- 합성 데이터와 실제 기관·개인정보를 혼동시키지 않는다.
- 내부 검토 메모와 청년에게 공개되는 회신을 분리한다.
- 권한이 없는 행동은 숨기거나 비활성화하고, 서버도 같은 권한을 다시 검증한다.

## 정보 구조 문법

### 핵심 탐색

좌측 주 메뉴는 아래 세 작업 경로만 유지한다.

1. `오늘` — 지금 처리할 회의, 결정·과제, 제안 피드백을 확인한다.
2. `회의·결정` — 일정·참석·회의록에서 담당자·기한이 있는 결정과 과제를 만든다.
3. `정책제안` — 근거, 피드백, 기관 전달, 회신·반영 경과를 한 기록에서 관리한다.

`위원회 정보`는 좌측의 현재 참여기구 컨텍스트 카드를 눌러 연다. 위원장은 그 화면에서 기본 정보를 관리하고, 일반 위원은 기본 정보를 열람하며 자신의 프로필을 수정한다. 구성원·권한, 운영 기록, 활동 리포트·인계는 일상 작업 경로가 아니므로 **위원장만** 좌측 하단의 작은 `위원회 관리` 아이콘 메뉴에서 연다. 이슈·공지는 이 메뉴와 핵심 탐색에서 제외한다.

모든 워크룸 메뉴는 가능한 한 아래 순서를 따른다.

1. 메뉴명
2. 현재 상태 또는 이 화면의 목적 한 줄
3. 주 행동과 핵심 작업 영역
4. 근거 자료 또는 최근 변경 이력

목록의 기본 열은 객체에 맞춰 아래에서 선택한다.

`대상 | 상태 | 담당 | 기한 | 마지막 변경 | 다음 행동`

## 시각 토큰

| 용도 | 값 | 원칙 |
| --- | --- | --- |
| 바탕 | `#FCFCFB` | 따뜻한 백색, 회색 면 남용 금지 |
| 패널 | `#FFFFFF` | 서로 다른 작업 단위에만 사용 |
| 본문 잉크 | `#203040` | 제목과 핵심 데이터, 로고의 짙은 남색 계열 |
| 보조 글자 | `#66747A` | 설명과 메타데이터 |
| 구분선 | `#DDE6E3` | 카드 그림자보다 우선 |
| 주 행동 | `#0B6B59` | 깊은 녹색의 행동 색 |
| 주 행동 강조 | `#075146` | hover·선택 상태 |
| 정보 강조 | `#4078B7` | 정보·진행 흐름 |
| 확인 필요 | `#AE7316` | 응답 대기·검토 필요 |
| 위험 | `#BF5B50` | 삭제·권한 회수·기한 위험 |
| 모서리 | `6–10px` | 제품 화면은 작고 단정하게 |
| 기본 간격 | `4/8/12/16/24/32px` | 임의 간격 대신 리듬 사용 |

좌측 탐색은 `#0B4A3D`를 바탕으로, 흰색 글자와 `#C1DDD3` 보조 글자만 사용한다. 선택 상태는 `#126351`의 더 밝은 녹색 면으로 구분하고, 본문 영역에서는 초록을 행동·완료 의미에만 제한한다.

### 색 사용 비율

- **중립 바탕·패널·글자: 약 82%** — 읽기와 기록을 위한 흰색, 옅은 회녹색, 짙은 잉크다.
- **구조색(좌측 탐색·주 행동): 약 12%** — 짙은 녹색과 선택 상태만 쓴다.
- **데이터·강조색: 약 6%** — 청록(정상·완료), 파랑(정보·진행), 황토(확인 필요), 산호(기한·위험)를 작은 면적에만 쓴다.

강조색을 큰 배경 면으로 확장하거나 한 화면에 세 가지 이상을 동등한 비중으로 쓰지 않는다. 상태는 색과 함께 텍스트·기호·수치를 제공한다.

### 데이터 시각화 규칙

- 숫자에 실제 비교 대상, 시간 흐름, 구성 비율이 있으면 표·텍스트만 반복하지 말고 막대, 진행선, 추세선, 구성 도표 중 가장 작은 적합한 방식으로 보여 준다.
- 분모가 없는 단일 건수는 차트처럼 꾸미지 않는다. 원본 기록 수, 마지막 변경, 대상 범위를 함께 표기한다.
- 운영 신호는 `전체 대상 중 현재 상태`를 작은 진행선과 문장으로 함께 보인다. 예: `전체 12건 중 응답 대기 3건`.
- 시간 변화는 4개 이상 시점이 있을 때만 추세선으로, 구성은 전체가 명확할 때만 도넛·막대로 표현한다. 개인 순위·열성도·점수 차트는 만들지 않는다.
- 시각화에는 제목, 단위, 기간 또는 분모, 텍스트 대체 정보를 포함한다. 색만으로 구분하지 않는다.

## 컴포넌트 규칙

- **좌측 탐색:** 데스크톱에서는 짙은 녹색의 둥근 고정 바로 두고, 본문 캔버스의 큰 둥근 좌측 경계와 맞물리게 한다. 맨 위에는 플랫폼 브랜드 마크를, 그 아래에는 로고 없는 현재 참여기구명·기수·소속기관을 둔다. 하단 계정은 고정한다.
- **상단 헤더:** 메뉴명 1개와 한 줄 설명. 반복 눈썹 문구·홍보 카피 금지.
- **패널:** 서로 다른 맥락을 구분할 때만 사용. 같은 종류의 항목은 행 목록으로 연결한다.
- **상태:** `● 검토 중`, `○ 답변 대기`, `✓ 회신 완료`처럼 텍스트와 기호를 함께 사용한다.
- **표:** 헤더는 작고 중립적으로, 행 전체를 클릭할 수 있게 하며 마지막 열에 다음 행동을 둔다.
- **빈 상태:** 무엇이 없는지, 누가 만들 수 있는지, 첫 행동이 무엇인지 말한다.
- **모달:** 해당 화면을 떠나지 않고 끝낼 수 있는 짧은 생성·수정에만 사용한다.
- **움직임:** 150–220ms의 짧은 전환으로 클릭과 화면 경계를 알린다. 처리 중인 것처럼 꾸미지 않는다.
- **홈 일정:** `참여기구 전체 일정`과 `내 워크룸 일정`을 나란히 둔다. 전체 일정에는 전체·분과 회의와 공통 기한을, 내 일정에는 참석 대상 회의·내 과제 기한·내 정책제안 마일스톤만 표시한다. 회의·과제·제안은 텍스트 범례와 서로 다른 표식으로 구분한다.

## 참조 화면에서 채택한 세부 문법

- **참여기구 컨텍스트:** 좌측에서는 참여기구 로고를 반복하지 않는다. 한 줄 기구명과 `기수 | 소속기관 (구분)` 맥락, chevron만 둔다. 이 영역은 참여기구 데이터 관리로 이어지는 진입점이다.
- **행 hover 행동:** 목록 전체는 열 수 있고, `열기 →`나 행 내부 보조 버튼은 hover·키보드 포커스 때만 선명해진다. 모바일에서는 숨기지 않는다.
- **고정 표 헤더:** 명단·조직·자료처럼 열이 많은 표는 헤더를 고정한다. 좁은 화면에서는 고정 대신 가로 스크롤을 허용한다.
- **필터 칩:** 상태나 기수처럼 소수의 즉시 판단 조건에만 쓴다. 기본값과 선택 상태가 한눈에 구분되어야 하며, 칩만으로 필수 기능을 감추지 않는다.
- **작성자 식별:** 사진이 없을 때는 이름 첫 글자 이니셜을 중성 원형으로 쓴다. 아바타는 관계와 작성자 구분을 돕는 용도이며 장식용 이미지를 넣지 않는다.
- **빈 상태:** 단색 선형 기호와 첫 행동 한 줄만 둔다. 감정적 일러스트나 제품 홍보 문구는 쓰지 않는다.

## 반응형·접근성

- 데스크톱에서는 좌측 탐색과 로그인 정보를 화면에 고정한다.
- 좁은 화면에서는 표를 억지로 카드화하지 않고 가로 스크롤 또는 핵심 열 우선순위를 쓴다.
- 키보드 포커스는 3px 외곽선으로 명확히 표시한다.
- 44px에 가까운 터치 영역, 본문 대비 4.5:1 이상, 색상 없이도 읽히는 상태를 목표로 한다.
- `prefers-reduced-motion`에서 애니메이션을 사실상 제거한다.

## 문구 점검

출시 전 모든 문구에 아래 질문을 적용한다.

- 이 문장이 없어지면 이용자가 잘못 판단하는가?
- 행동의 대상과 결과가 분명한가?
- 실제 권한과 시스템 상태를 과장하지 않는가?
- 행정 용어를 쓰더라도 다음 행동을 이해할 수 있는가?

세 질문에 모두 `아니오`인 소개 문구는 삭제한다.

## 화면 수락 기준

- 5초 안에 현재 상태, 다음 행동, 근거 기록의 위치를 찾을 수 있다.
- 같은 뜻이 제목·설명·패널 안에서 반복되지 않는다.
- 카드보다 목록·표·이력이 우세하다.
- 화면을 흑백으로 보아도 상태와 우선순위를 구분할 수 있다.
- 실제 데이터가 없을 때 합성·예시·미설정 상태가 명확하다.
- 데스크톱, 900px, 390px에서 주요 행동이 잘리거나 겹치지 않는다.

## KRDS 웹 타이포그래피·정보 위계 우선 기준

- 운영 기록의 본문은 최소 `16px`(`1rem`)과 `1.5` 이상의 상대 행간을 사용한다. 설명·메타·상태·조작 요소는 본문과 구분하되, 실제 판단이나 조작에 쓰이는 텍스트는 `14px` 미만으로 줄이지 않는다.
- 굵기는 본문 `400`, 제목·행동 강조 `700`을 기본으로 하며, 역할 구분을 위해 과도한 굵기 단계를 만들지 않는다.
- 페이지 제목(H1), 작업 단위 제목(H2), 패널·목록 제목(H3), 본문·메타의 순서로 위계를 고정한다. 상태·권한·기한은 색만이 아니라 텍스트로 전달한다.
- 구현에서는 `public/additional.css`의 마지막 KRDS 레이어가 이전 화면별 보정값보다 우선한다. 새 화면을 만들거나 수정할 때 이 레이어를 우회해 작은 글자를 추가하지 않는다.
