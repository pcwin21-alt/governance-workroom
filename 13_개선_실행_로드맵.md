# 거버넌스 워크룸 개선 실행 로드맵

2026-08-26 기준. 이 문서는 로컬 MVP에 실제로 반영한 항목과, 외부 권한·운영 결정이 있어야 하는 항목을 구분한다.

## 이번 반영

- 역할별 홈의 `내가 지금 할 일`·`오늘 처리할 운영 항목`·`운영 우선순위` 구조를 유지한다. 수치 카드는 다음 행동과 연결하고, 개인 순위나 열성도 점수는 만들지 않는다.
- 계약형 테넌트는 `active` 상태이고 이용 기간 안에 있을 때만 운영 기록을 저장한다. `proposal_pending`, `paused`, `expired`, 시작 전·종료 후 기간은 서버에서 차단한다.
- 로그인 실패는 주소와 식별자 조합별 15분 동안 10회까지 허용하고, 초과하면 `429`와 `Retry-After`를 반환한다. 성공 로그인은 실패 카운터를 초기화한다.
- 계약 활성화 전·활성화 후·기간 종료 후의 기록 저장 차단과 로그인 속도 제한을 자동 테스트로 추가했다.

## 다음 로컬 구현 순서

1. 역할별 홈에서 목록 행을 해당 객체의 필터 화면으로 연결하고, 빈 상태마다 조건·담당 역할·다음 행동을 표시한다.
2. 첫 로그인 역할 안내와 저장·배정·권한 변경 뒤 확인 위치를 제공한다. 안내는 닫고 다시 열 수 있어야 한다.
3. 일반위원·간사·의장단·운영사 과업 테스트를 기록한다. 성공률, 완료 시간, 막힌 지점, 용어 오해를 남기며 미관 평가는 보조 신호로 둔다.
4. 감사 로그를 관리형 이벤트 저장소로 옮길 때 행위자·권한 근거·전후 값·보존기간·반출 이력을 함께 설계한다.

## 실제 파일럿 전 차단 항목

- 관리형 관계형 DB, 암호화, 백업·복구, 접근통제, 비밀관리, HTTPS, MFA/SSO, 독립 보안 테스트
- 기관별 개인정보 처리자·수탁자 역할, 처리방침, 보존·파기와 정보주체 권리 절차
- 실제 파일 업로드의 악성 파일 검사·권한별 다운로드·반출 승인
- 실제 이메일·메신저의 수신 동의, 발신 인증, 수신 거부, 실패·재발급 이력
- AI 외부 전송의 입력 필터, 국외 이전·보존·비용 한도, 사람 검토 책임
- YouthTogether 연동의 읽기 전용 서비스 계정, 최소 권한 토큰, 상태 API 계약, 독립 검증

이 항목들은 로컬 데모에서 성공했다고 표시하지 않는다. 필요한 외부 자격·운영 결정이 확인된 뒤 별도 구현·검증한다.
