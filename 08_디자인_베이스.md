# 거버넌스 워크룸 디자인 베이스

## 제품의 인상

`이어지는 기록` — 청년참여기구가 회의의 판단을 정책제안과 다음 기수의 행동으로 넘겨 주는 작업대.

보여 주어야 할 것은 젊어 보이는 장식이 아니라, 기록이 정리되어 있고 다음 행동이 보인다는 감각이다.

## 기본 토큰

| 층 | 값 | 쓰임 |
| --- | --- | --- |
| Paper | `#F7F8FC` | 전체 배경과 긴 읽기 면 |
| Ink | `#17213A` | 본문, 구조, 워크룸 사이드바 |
| Link Blue | `#4056D4` | 주 행동, 선택 상태, 연결 |
| Warm Surface | `#F7F1EB` | 원칙·안내처럼 사람의 판단을 설명하는 면 |
| Line | `#E0E6EF` | 얇은 정보 경계 |

## 화면 문법

- 제품명은 텍스트로만 표기한다. 브랜드 확정 전 이니셜 마크는 쓰지 않는다.
- 제목은 `정책제안을 검토합니다`, `회의 결과를 기록합니다`처럼 과업으로 바로 시작한다.
- 작은 영어 라벨과 장식용 `01–04`는 사용하지 않는다.
- 실제 체크리스트가 아닌 한 숫자보다 제목·설명·상태 태그를 쓴다.
- 카드에는 한 과업 또는 한 기록 묶음만 둔다. 그림자는 미세하게, 행동색은 주 CTA와 현재 위치에만 쓴다.
- 공개 화면은 제품 흐름을 먼저 보여 주고, 소개·원칙·도입 조건은 그 뒤에 둔다.

### 배경 패턴

- 공개 홈에는 `기록의 좌표면`을 사용한다. Paper 위에 매우 낮은 대비의 34px 격자, Link Blue의 옅은 연결 원·선, Warm Surface의 확산 면을 겹친다.
- 이 패턴은 회의→결정→정책제안→경과의 연결을 암시할 뿐, 데이터 시각화나 장식 이미지처럼 읽히면 안 된다.
- 워크룸·폼·정책제안 본문처럼 긴 글과 입력이 중심인 화면에는 적용하지 않는다. 가독성과 오류·상태 구분이 우선이다.
- 패턴의 불투명도는 본문보다 항상 낮게 유지한다. 정보 계층을 설명할 수 없는 패턴은 추가하지 않는다.

### 메뉴별 레이아웃 변주

공통 토큰·제목 체계·버튼 위계는 유지하되, 모든 메뉴를 같은 제목+본문+카드 격자로 만들지 않는다.

| 화면 성격 | 레이아웃 문법 |
| --- | --- |
| 이슈·사례 | 편집형: 넓은 문제 제기, 설명 열, 대표 사례 1개와 오프셋 보조 사례 |
| 운영 흐름 | 경로형: 우측 정렬 도입부와 단계별 높이가 달라지는 오프셋 카드 |
| 기구별 사용 장면 | 교차 목록형: 좌·우 진입점이 번갈아 나타나는 긴 읽기 목록 |
| 운영·보안 기준 | 기준표형: 핵심 원칙 1개를 넓게 두고 보조 기준을 묶음 |
| 제품을 만드는 이유 | 선언형: 큰 문제 카드와 짧은 보조 판단 카드 |
| 파일럿 | 대화형: 준비 체크와 시작 장면 점검 폼을 나란히 둠 |

모바일에서는 모든 변주를 단일 열로 되돌리고, 읽기 순서와 행동 순서를 우선한다.

## 컴포넌트 기준

| 컴포넌트 | 기본 형태 | 금지 |
| --- | --- | --- |
| 버튼 | 44px 이상, 명확한 행동 동사 | `자세히 보기`만 있는 모호한 주 CTA |
| 상태 | 텍스트 태그 + 설명 | 색·아이콘만으로 의미 전달 |
| 카드 | 14–16px 반경, 1px 경계, 낮은 그림자 | 유리 질감, 과도한 떠오름 |
| 타이포 | Pretendard, 본문 16/24, 좌측 정렬 | 과한 대문자·초굵은 영문 라벨 |
| 절차 | 실제 순서일 때만 숫자 | 장식형 로드맵 번호 |

### 상호작용 모션

- 클릭은 `150ms` 안팎의 눌림으로만 확인한다. 버튼·카드·메뉴가 위로 뜨거나 크게 흔들리지 않게 `1px` 이동과 아주 짧은 색·경계 전환을 쓴다.
- 메뉴·상세 화면 전환은 새 내용이 `210ms` 안팎으로 아래에서 짧게 들어온다. 저장·제출·권한 변경에는 이 전환을 처리 중인 것처럼 쓰지 않고, 실제 완료·실패 상태 문구로 결과를 알린다.
- 모달은 배경과 본문을 분리해 열고 닫는다. 즉시 제거하지 않되, 입력을 막거나 기다리게 하는 장식형 로딩은 만들지 않는다.
- OS의 `동작 줄이기` 설정에서는 애니메이션과 전환을 사실상 제거한다.

## 이번 제작 범위

공개 소개와 합성 데모 워크룸에 이 기준을 먼저 적용한다. 실제 로고, 사진, 테넌트별 화이트라벨은 브랜드·운영 정책이 확정될 때까지 보류한다.
