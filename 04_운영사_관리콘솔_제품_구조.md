# 운영사 관리 콘솔(Control Plane) 제품 구조

## 목적

운영사 관리 콘솔은 각 청년참여기구가 사용하는 워크룸과 분리된 운영자 공간이다. 운영자는 개별 위원의 활동량을 평가하지 않는다. 대신 테넌트의 운영 상태, 정원, 권한, 제안·컨설팅 병목, 사례 공개 동의, 연구 데이터 요청, 보안·보존 정책의 준비 상태를 확인하고 지원한다.

이 문서는 현재 구현의 정보 구조와 실제 도입 전 확장 경계를 함께 기록한다.

## 공간 분리

| 공간 | 주 사용자 | 목적 | 현재 진입 방식 |
| --- | --- | --- | --- |
| 운영사 관리 콘솔 | 운영사 관리자 | 다기관 설정·지원·감사·보안 현황 | 좌측 메뉴 `운영사 콘솔` |
| 테넌트 워크룸 | 간사, 의장단, 위원, 검토자 | 회의·제안·피드백·제출·인수인계 | 좌측 메뉴 또는 콘솔의 `워크룸 열기` |

주소 직접 진입을 위한 `/admin/*`, `/workspace/*` 정적 진입점은 제공하지만, 현재 MVP는 탭 기반 화면 상태로 작동한다. 실제 다기관 배포에서는 테넌트 슬러그와 서버 측 권한 검증을 결합한 명시적 라우팅으로 교체한다.

## 운영사 콘솔 메뉴

| 메뉴 | 운영 판단 | 현재 MVP |
| --- | --- | --- |
| 운영 개요 | 운영 중 기구, 구성원, 제안, 중지·미배정·승인대기 신호 | 구현 |
| 도입 문의 | 공개 파일럿 문의, 이메일 알림 연결 상태, 접수·검토·연락·종료 상태 | 구현; 실제 CRM·자동 회신은 후속 |
| 테넌트 | 생성, 중지·재개, 정원, 템플릿, 기능 플래그, 이용 구조·후원 주체·좌석·기간·지원 범위, 워크룸 진입 | 구현; 계약·결제는 후속 |
| 구성원·권한 | 소속, 역할, 임기, 초대·접근 상태 확인 | 조회 구현; 운영은 워크룸에서 수행 |
| 공통 콘텐츠 | 이슈·공지의 출처/확인일/검토·공개 범위 | 합성 검토 큐 조회 |
| 제안·컨설팅 | 전 테넌트 제안 상태와 검토자 배정 대기 | 조회 구현 |
| 사례·협업 | 작성자 동의와 운영사 승인 분리 | 합성 승인 큐 조회 |
| 연구·데이터 | 목적·변수·보관기한·승인 상태 | 요청 생성과 승인 대기 기록 구현 |
| 보안·서비스 | 동의 버전, 보존 초안, 삭제 요청, 감사 로그 | 조회 구현 |

## 핵심 엔터티

| 엔터티 | 중요한 필드 | 경계 |
| --- | --- | --- |
| PlatformOperator | 사용자, 역할, 감사 수행자 | `platform_admin`만 콘솔 API 접근 |
| Tenant / TenantSettings / ServiceAccess | 이름, 기수, 정원, 상태, 템플릿, 기능 플래그, 이용 방식·후원 좌석·기간·지원 범위, 분과·연령대 설정 | 모든 워크룸 데이터의 `tenantId` 경계; 실제 운영은 기관 계약 또는 후원 좌석 전제 |
| TenantTemplate | 역할, 연령대 선택지, 분과 기본값 | 새 기구 생성의 출발점 |
| User / Membership / Invitation | 역할, 임기, 소속, 활성 상태, 초대 상태 | 실제 이메일 발송·본인 인증은 후속 |
| ContentItem / ContentReview | 출처, 확인일, 메모, 공개 범위, 검토 상태 | 이슈·공지의 운영 사실과 출처를 구분 |
| Proposal / ConsultationAssignment | 파이프라인 상태, 검토자, 기한, 피드백 | AI가 채택 판단·자동 제출하지 않음 |
| CasePublicationRequest / CollaborationRequest | 작성자 동의, 운영사 승인, 공개 범위 | 공개 전 단계 분리 |
| ResearchRequest / ResearchExportRequest | 목적, 변수, 만료, 승인자, 반출 기록 | 현재 반출 승인·비식별화는 미구현 |
| ConsentVersion / RetentionPolicy / DeletionRequest | 버전, 시행일, 보존 초안, 요청 처리 상태 | 실제 법무·계약·파기 절차는 미확정 |
| AuditLog / SystemNotice | 수행자, 자원, 시각, 서비스 공지 | 본문 개인정보는 로그에 기록하지 않음 |
| PilotRequest | 기구명, 담당자, 업무용 연락 수단, 운영 장면, 문의 처리 동의, 접수 상태·알림 결과 | 운영사 관리자만 열람; 마케팅·테넌트 데이터와 분리 |

`Membership`, `PermissionPreset`, `ContentItem`, `ResearchExportRequest`, `CollaborationRequest`는 현재 JSON 모델에서 역할·게시물·요청 필드로 최소 표현한다. 실제 DB 전환에서는 독립 테이블과 FK·변경 이력으로 정규화한다.

## 권한 경계

- 운영사 관리 API(`/api/admin/*`)는 서버에서 `platform_admin` 역할을 다시 검사한다.
- 일반위원을 포함한 비운영사 역할은 콘솔 화면을 보더라도 API에서 `403`으로 차단된다.
- 운영사 관리자는 테넌트 상태를 중지·재개할 수 있으며, 중지된 테넌트의 일반 사용자는 API 접근이 차단된다.
- 외부 검토자는 지정된 정책제안서만 받을 수 있다.
- 모든 생성·설정 변경은 감사 로그에 남긴다.

## 데이터·개인정보 원칙

- 합성 데모 외 실제 기관명, 정책자료, 개인 정보는 입력하지 않는다.
- 전체 생년월일과 민감정보는 수집하지 않는다. 연령대, 직업, 시군구는 선택형이다.
- 연구 데이터 추출은 목적·변수·승인을 먼저 기록한다. 승인·비식별화·반출 이력 구현 전에는 외부 활용하지 않는다.
- 개인 열성도 점수·랭킹·자동 평가 기능은 만들지 않는다.
- 무기한 무료 워크룸은 제공하지 않는다. 템플릿·합성 데모는 실제 자료 저장·운영·지원·보고의 대체물이 아니며, 후원 좌석은 후원 기관이 비용을 부담하는 계약형 이용 구조다.

## 실제 도입 전 외부 결정

1. 테넌트별 계약 주체, 개인정보 처리 역할, 운영자 권한 승인 절차
2. 보존기간·기록물 해당 여부·삭제/백업 파기·정보주체 통지 방식
3. SSO 또는 이메일 인증, MFA, 도메인·화이트라벨 범위
4. 공통 콘텐츠·사례 공개·연구 반출에 필요한 기관 승인과 동의 문구
5. AI 기능을 실제 연결할 경우 모델, 비용, 입력 데이터, 보존, 사람 검토와 책임 경계
6. 기관 계약·후원 좌석의 계약 주체, 비용 부담 주체, 정원·기간·갱신·종료 시 데이터 처리, 지원 수준과 응답 책임
7. 파일럿 문의의 보존·삭제, 운영자 접근권한, 발신 도메인, 이메일 발송 위탁과 실제 알림 전달 확인
