# 청년거버넌스 워크룸 전면 디자인 개편안

> 구현 정본: [`DESIGN_SYSTEM.md`](./DESIGN_SYSTEM.md). 이 문서는 개편의 화면 구조와 전환 범위를 설명하고, 색·간격·표기·접근성 판단은 디자인 가치관 문서를 따른다.

## 결정

현재의 `짙은 좌측 바 + 패널·카드 중심` 화면을 종료한다. 새 기준은 **운영 기록을 빠르게 읽고 다음 조치를 수행하는 공공 업무 도구**다. 참고 화면의 미니멀한 SaaS 구조는 가져오되, 이용률·파워 유저 같은 일반 SaaS 분석 대시보드 문법은 사용하지 않는다.

핵심 흐름은 다음 한 줄로 고정한다.

`이번에 확인할 일 → 근거가 되는 회의·제안 → 담당자·기한 → 처리 이력`

## 레퍼런스에서 채택·제외할 것

| 관찰 | 적용 | 제외 |
| --- | --- | --- |
| 밝은 고정 사이드바와 넓은 메인 캔버스 | 밝은 좌측 탐색, 얇은 구분선, 고정된 계정 영역 | 브랜드를 대체하는 다크 배경·큰 기구 카드 |
| 상단의 짧은 인사와 상태 스트립 | 현재 기수·이번 주 처리할 일을 한 줄 작업 스트립으로 표시 | 환영 문구, 제품 홍보, 반복 안내 |
| 행 중심 활동 이력 | 누가·무엇을·언제 바꿨는지 표/타임라인으로 표현 | 카드마다 긴 소개문과 상태 배지 남발 |
| 수치와 시각화 | 기한, 답변 대기, 참석 응답처럼 판단을 돕는 수치만 표시 | 열성도 점수, 개인 순위, 근거 없는 KPI 그래프 |
| 액션 버튼의 절제 | 화면당 주 행동 1개, 보조 행동은 행 내부 또는 점 메뉴 | 같은 우선순위의 큰 버튼 다수 |

## 공통 레이아웃 규칙

### 1. 정보 위계

모든 메뉴는 아래 순서만 쓴다.

1. 메뉴명
2. 지금 해야 할 일 또는 현재 상태 1줄
3. 핵심 작업 영역
4. 최근 변경 이력

`메뉴명 + 기능 설명 + 같은 뜻의 섹션 제목` 반복은 없앤다. 설명은 기한, 권한, 빈 상태, 복구 행동처럼 없어지면 오해가 생기는 정보만 남긴다.

### 2. 표기 문법

- 객체명은 굵게: `2기 제3차 정기회의`
- 상태는 텍스트+점: `● 검토 중`, `○ 답변 대기`, `✓ 회신 완료`
- 책임은 `담당 박진감`, 기한은 `9월 3일 전`처럼 사람이 읽는 언어로 쓴다.
- 기록은 `박진감이 정책제안 전달문을 만들었습니다 · 9월 1일 10:20` 형식으로 통일한다.
- 버튼은 행동+대상: `참석 응답`, `검토 의견 남기기`, `통합 자료 만들기`.
- `보기`, `저장`, `관리`, `처리`처럼 대상이 없는 버튼은 제거한다.

### 3. 표면과 밀도

- 배경: 따뜻한 백색. 전체를 회색 카드로 감싸지 않는다.
- 구분: 1px 선과 20~28px 간격을 우선한다.
- 카드: 서로 다른 작업 단위가 만날 때만 사용한다. 같은 목록의 행은 표/리스트로 보여 준다.
- 모서리: 8px 내외. 큰 둥근 모서리는 로그인·제품 소개 등 제한된 장면에서만 사용한다.
- 색: 잉크, 중성 회색, 짙은 청록(주 행동), 흐린 남색(마감·검토 강조)만 사용한다. 상태는 색만으로 표현하지 않는다.

### 4. 좌측 탐색

- 폭 248px의 밝은 고정 바.
- 상단: 로고가 있으면 로고, 없으면 작은 `등록 요망` 영역. 그 옆에 참여기구명 한 줄.
- 그 아래: `2기 | 행정안전부 (중앙정부)`처럼 소속 맥락 한 줄.
- 메뉴는 아이콘+명칭만. 현재 메뉴에는 옅은 배경과 좌측 2px 강조선.
- 하단 고정: 알림, 첫 사용 안내, 로그인한 사람 이름·직책, 로그아웃.

## 메뉴별 전환

| 메뉴 | 첫 화면 | 주 작업 | 하단/보조 영역 |
| --- | --- | --- | --- |
| 내 워크룸 | 이번 주 처리 스트립 | 다음 회의 / 검토 중 제안 / 기관 회신 3개 작업 목록 | 최근 기록 표 |
| 회의·일정 | 월간 캘린더 | 선택한 회의의 참석·안건·결정 | 회의록·출결 이력 |
| 조직도·분과 관리 | 계층 조직도 | 선택 분과의 구성원·활동·미배치 인원 | 변경 이력 |
| 전체 명단 관리 | 밀도 높은 구성원 표 | 초대, 역할·권한, 분과 배치 | 참석·제안 건수 바로가기 |
| 정책제안 | 처리 단계 목록 | 제안 상세의 근거·피드백·행정 환류 | 결정·회신 이력 |
| 이슈·공지 | 기관 보도자료/공지 피드 | 공지 발행, 원문 공유 | 수집·발행 이력 |
| 통합 자료 만들기 | 생성한 자료 목록 | 기록 선택→문서 초안→DOCX/PDF | 원본 보관 기록 드라이브 |
| 기수 인계·리포트 | 미결 제안·인계 준비 목록 | 인계 패키지, 기간 리포트 | 이전 기수 이력 |
| 참여기구 데이터 관리 | 기본 정보 요약 | 대표·임기·소속기관·바로가기 수정 | 변경 이력 |
| 내 정보·데이터 | 개인 프로필 | 사진·선택 정보 수정 | 작은 계정/데이터 권리 진입점 |

## 핵심 화면 예상

### 내 워크룸

상단에 큰 환영 문구 대신 `내 워크룸`과 기수 정보만 둔다. 바로 아래에 세 칸짜리 진행형 스트립을 둔다.

`회의 응답 1건 → 정책제안 검토 2건 → 기관 회신 확인 1건`

각 칸은 상태, 마감, 가장 가까운 행동 하나만 보여 준다. 그 아래는 차트가 아니라 실제 처리할 업무 목록이다. 가장 아래의 최근 기록 표가 누가 무엇을 했는지 증명한다.

### 정책제안

제안서는 카드 모음이 아니라 행정 이력 목록이다.

`제안명 | 상태 | 소관 부서 | 다음 기한 | 마지막 기록 | 다음 행동`

상세에서는 `문제·근거·제안·검토·결정·회신`을 시간 순서로 놓는다. 내부 검토와 청년 공개 회신은 같은 화면에서 분리 표시한다.

### 통합 자료 만들기

상단에 생성물 목록, 아래에 원본 보관 기록을 둔다. 생성 버튼을 누르면 문서 종류→수신처→선택 기록 순으로 고르고, 만들기 직전에 포함 기록 수와 문서 결과를 확인한다. DOCX와 PDF 저장은 문서 행의 보조 행동으로 둔다.

## 일괄 구현 순서

1. 전역 토큰, 좌측 바, 상단 헤더, 목록·상태·버튼 표기 문법 교체
2. 내 워크룸과 정책제안을 새 정보 구조의 기준 화면으로 구현
3. 회의·명단·조직도·이슈·통합 자료·인계를 같은 표/이력/작업 스트립 문법으로 이행
4. 권한·빈 상태·오류·모바일에서 같은 위계가 유지되는지 점검

## 수락 기준

- 화면마다 이용자가 5초 안에 `현재 상태`, `다음 행동`, `근거 기록 위치`를 찾는다.
- 같은 내용이 제목·설명·카드에 세 번 반복되지 않는다.
- 카드보다 목록·표·이력이 우세하며, 사용자 업무의 순서가 보인다.
- 색이 없어도 상태·권한·마감이 구분된다.
- 개인 순위나 열성도 점수는 새 디자인에도 포함하지 않는다.

## 이번 시안의 경계

첨부 이미지는 참고용 UI 패턴이며 실제 제품·기관 데이터가 아니다. 시안은 방향 검증용이다. 브랜드명, 실제 로고, 실제 기관 사례, 유료 플랜 표시는 확정 전 넣지 않는다.
