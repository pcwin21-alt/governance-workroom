# 브랜드 아이덴티티·디자인 시스템 제안

## 결정 요약

브랜드명은 확정하지 않는다. 현 단계에서는 제품을 설명하는 임시명 `거버넌스 워크룸`을 유지하고, 시각 아이덴티티는 **기록이 다음 행동으로 이어지는 공공의 작업대**라는 개념으로 고정한다.

기존의 짙은 녹색은 안정적이지만 환경·복지·일반 행정 도구로 읽힐 가능성이 있다. 청년참여기구의 토론, 연결, 수정, 전달이라는 성격에는 **딥 인디고 + 링크 블루 + 따뜻한 중립색**이 더 선명하다.

## 1. 아이덴티티 개념: `이어지는 기록`

이 제품은 회의 앱도, 정책 뉴스 서비스도, 구성원 관리 도구도 아니다. 의견이 사라지지 않고 다음 사람의 판단과 행동으로 이어지게 하는 기록 인프라다.

### 브랜드가 보여 줄 태도

| 원칙 | 화면에서의 번역 | 하지 않을 것 |
| --- | --- | --- |
| 연결 | 회의→결정→제안→경과를 선형 흐름으로 표시 | 단순 알림·기능 모음처럼 분절하지 않음 |
| 추적 가능성 | 출처, 확인일, 수정 이유, 제출 경과를 보이게 함 | ‘AI가 알아서 한다’는 블랙박스 연출 |
| 참여의 존엄 | 역할별 다음 행동을 제시하고 개인을 평가하지 않음 | 열성도 점수·출석률 순위·게임화 배지 |
| 공공의 온도 | 차갑지 않은 여백과 언어, 검증 가능한 한계 공개 | 국가기관 같은 권위주의적 인장·감사 연출 |

**핵심 문장**: `청년의 의견이 다음 행동으로 이어지도록 기록합니다.`

공개 홈의 주 문장인 “회의에서 나온 청년의 의견이 제안서와 다음 행동으로 남도록 관리합니다.”는 제품 효용을 말한다. 위 핵심 문장은 소개·브랜드 설명·문서의 낮은 농도에서만 쓴다.

## 2. 컬러 시스템: Link Blue

### 기본 배분

- 60%: 차갑지 않은 밝은 중립 면 (`Paper`, 흰 카드)
- 30%: 딥 인디고 구조색 (`Ink`, 사이드바·중요 구획·텍스트)
- 10% 이하: 링크 블루 (`Link Blue`, CTA·활성 상태·핵심 연결)

이 비율은 출발 가설이다. 실제 접근성 대비와 사용성 검증 후 조정한다.

| 토큰 | 값 | 역할 | 사용 원칙 |
| --- | --- | --- | --- |
| `--paper` | `#F7F8FC` | 전체 배경 | 넓은 면, 읽기 여백 |
| `--panel` | `#FFFFFF` | 카드·입력 표면 | 정보 묶음 |
| `--ink` | `#17213A` | 구조·본문·다크 면 | 사이드바, 긴 문장, 중요한 구획 |
| `--muted` | `#667085` | 보조 설명 | 메타 정보, 설명 문구 |
| `--line` | `#E0E6EF` | 구분선 | 경계는 얇고 조용하게 |
| `--brand` | `#4056D4` | 링크 블루 | 주 CTA, 활성 탭, 현재 위치 |
| `--brand-dark` | `#2D3E9E` | 링크 블루의 깊은 상태 | hover, 링크 텍스트 |
| `--accent` | `#EEF0FF` | 선택·보조 강조 | 활성 배경, 단계 표시 |
| `--warm-surface` | `#F7F1EB` | 인간적 호흡 | 안내·원칙·파일럿 보조 면; CTA 금지 |
| `--danger` | `#B42318` | 파기·중지·위험 | 파괴적 행동에만 사용 |

보조 상태색은 나중에 접근성 검토 후 도입한다. 성공·경고·오류를 장식색으로 과도하게 쓰지 않고, 상태명과 함께 쓴다.

## 3. 시각 언어

### 형태

- 카드와 목록은 `연결된 기록 묶음`처럼 보이게 한다. 강한 그림자·유리 질감·3D 효과는 쓰지 않는다.
- 선은 `1px`, 반경은 카드 `14–16px`, 버튼 `10px`을 기본으로 둔다.
- 회의→결정→제안의 단계는 숫자, 얇은 연결선, 상태 문구로 표현한다. 화살표나 로드맵 장식을 남발하지 않는다.
- 아이콘은 선명한 단색 선형 아이콘만 사용한다. 정부 휘장, 체크 배지, 번개, 트로피는 기본 모티프로 쓰지 않는다.

### 타이포그래피

- 기본: `Pretendard`, `Noto Sans KR`, 시스템 산세리프.
- 공개 홈 H1: `36–48px/700`, 업무 화면 H1: `28–32px/700`, 본문: `16px/24px/400`.
- 긴 정책·회의 내용은 크기를 줄여 우겨넣지 않는다. 정보를 접거나 문서 단위로 나눈다.
- 굵기 `800+`은 브랜드명을 포함해 희소하게만 사용한다.

### 제목·라벨의 문법

- 공개 화면은 영어 보조 라벨(eyebrow) 뒤에 한국어 제목을 붙이지 않는다. 블록은 사용자가 바로 이해할 수 있는 한국어 제목으로 시작한다.
- 브랜드명이 확정되기 전에는 임시 이니셜 마크를 제목 옆에 붙이지 않는다. 현재는 텍스트 제품명만 쓴다.
- `01`, `02` 같은 장식형 번호는 삭제한다. 번호는 순서가 행동의 의미를 바꾸는 실제 절차·체크리스트에서만 쓴다.
- 분류가 필요한 경우에도 추상적 영어 대신 `이슈 예시`, `검토 필요`, `제출 완료`처럼 실제 정보 상태를 말한다.

### 사진·일러스트 방향

- 초기 공개 페이지는 장식 사진이 없어도 된다. 실제 UI 흐름과 기록 예시가 더 정직한 신뢰 근거다.
- 이미지를 추가할 때는 청년의 얼굴을 상징물처럼 소비하지 않는다. 회의 준비물, 메모, 공동 검토, 지역의 실제 맥락처럼 `일이 이루어지는 장면`을 우선한다.
- 실제 참여자의 사진·발언·사례를 쓸 때는 공개 동의를 별도로 받는다.

## 4. UX 언어

문구는 브랜드 감탄사보다 조건과 행동을 먼저 둔다.

| 목적 | 권장 | 피할 표현 |
| --- | --- | --- |
| 첫 행동 | `우리 기구 운영 흐름 보기` | `새로운 거버넌스를 경험하세요` |
| 빈 상태 | `예정된 회의가 없습니다. 회의 일정을 등록하세요.` | `아직 활동이 없어요` |
| 권한 | `이 화면은 현재 역할로 열 수 없습니다.` | `접근 오류` |
| AI 보조 | `구조 점검 질문 보기` | `AI가 최적의 정책을 제안합니다` |
| 제출 | `최종 제출 기록 남기기` | `제출 완료 보장` |

## 5. 공간별 적용

| 공간 | 주된 인상 | 색·밀도 적용 |
| --- | --- | --- |
| 공개 소개 | 침착한 설득, 업무 흐름의 선명함 | 넓은 Paper, Link Blue CTA 하나, Warm Surface 보조 섹션 |
| 테넌트 워크룸 | 오늘 처리할 일을 찾는 집중 | Ink 사이드바, Paper 본문, Blue 활성·저장 행동 |
| 정책제안 | 논증과 검토의 집중 | 더 넓은 본문 폭, 근거/피드백을 얇은 선으로 구분 |
| 운영사 Control Plane | 사람 감시가 아닌 지원 병목 파악 | 표·상태의 밀도는 높이고, 경고색은 실제 위험에만 사용 |

## 6. 이번 반영 범위와 보류

### 이번 반영

- 전역 토큰을 녹색에서 Link Blue 체계로 전환
- 사이드바, 공개 소개, 카드·활성 상태·포커스·안내 면을 같은 팔레트로 정렬
- 공개 사이트의 Warm Surface를 보조 면으로 사용해 공공 서비스의 차가움을 완화
- 기존 기능·권한·카피 구조는 유지
- 공개 소개·데모·운영사 화면에서 영어 보조 라벨, 임시 이니셜 마크, 장식형 번호를 제거

### 보류

- 최종 브랜드명, 로고, 도메인, 워드마크
- 실제 사진·일러스트 제작과 참여자 공개 동의
- 상태색의 WCAG 도구 기반 대비 판정과 실제 모바일 기기 시각 QA
- 테넌트별 화이트라벨 색상 허용 범위

브랜드명과 로고가 확정되기 전에는 Link Blue를 제품의 독점적 상징으로 과장하지 않는다. 이는 제품 역할을 구분하기 위한 임시 디자인 시스템이다.
